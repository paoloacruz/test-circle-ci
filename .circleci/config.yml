version: 2
jobs:
  build:
    docker: # See https://docs.docker.com/get-started/#docker-concepts if you are new to Docker <https://docs.docker.com/get-started/#docker-concepts%20if%20you%20are%20new%20to%20Docker> .
      - image: circleci/ruby:2.4.1-jessie
    steps:
      - checkout
      - run: echo "A first hello"
